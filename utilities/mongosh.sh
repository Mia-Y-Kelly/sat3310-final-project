# Utility script to connect to the database
# Connect when authentication is disabled
# mongosh "mongodb://ip-address27017";

# Connect with authentication is enabled
# mongosh "mongodb://ip-address:27017" --username admin --authenticationDatabase admin;

# Useful commands
# db.getCollectionInfos() --> returns all collections
# db.Course.find({}) --> returns all documents in Course collection
# db.Student.find({}) -- returns all documents in Student collection
